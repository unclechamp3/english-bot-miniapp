services:
  - type: web
    name: english-bot-api
    runtime: python
    buildCommand: cd api && pip install -r requirements.txt
    startCommand: cd api && python start.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PYTHONPATH
        value: /opt/render/project/src

